{
  "name": "scriptorium-dashboard-qa",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright regression harness for the Scriptorium telemetry dashboard.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
